{
  "name": "create-campfire",
  "version": "0.0.1",
  "description": "Scaffold a Campfire presentation repository (bun create campfire my-deck)",
  "type": "module",
  "bin": {
    "create-campfire": "./dist/index.mjs"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@campfire/typescript-config": "workspace:*",
    "@types/node": "^25.9.2",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3"
  }
}
